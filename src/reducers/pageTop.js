const initialState = {
  headerText: {
    ru: "Конструктор последовательностей асан хатха-йоги"
  },
  instructionText: {
    ru: "Перетаскивай карточки из списка асан в панель последовательности"
  }
};

export function pageTopReducer(state = initialState, action) {
  return state;
}
